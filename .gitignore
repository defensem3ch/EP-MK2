build/
# JUCE is a submodule now, not a symlink into the MK1 project next door.
# It was the latter, which meant this could not be built without a frozen
# project present, and a clone got no JUCE at all.
*.MID
*.mid

# Reference material we are allowed to analyse but not redistribute:
# commercial sample libraries and paywalled papers.  Analysis *results*
# are ours and may be committed.
*.xrni
samples/
research/
