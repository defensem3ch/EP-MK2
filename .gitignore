build/
dist/

# Credentials never belong in the tree.
*token*
dumps/
*.pem

# JUCE is a submodule, not a symlink into a neighbouring project.
*.MID
*.mid

# Reference material we may analyse but not redistribute.
# Analysis *results* are ours and may be committed.
*.xrni
samples/
research/
